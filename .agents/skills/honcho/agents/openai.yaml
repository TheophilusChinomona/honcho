interface:
  display_name: "Honcho"
  short_description: "Repo-specific patterns and workflows for honcho"
  default_prompt: "Use the honcho repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true